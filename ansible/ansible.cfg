[defaults]
inventory = inventory.ini
remote_user = ubuntu
private_key_file = ~/.ssh/devops-project-key.pem
host_key_checking = False
timeout = 30
retry_files_enabled = False

[privilege_escalation]
become = True
become_method = sudo
become_user = root
